import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dingo.fkerni',
  appName: 'Fkerni',
  webDir: 'dist',
  backgroundColor: '#070a11',
  android: {
    backgroundColor: '#070a11'
  },
  ios: {
    backgroundColor: '#070a11'
  }
};

export default config;
